#!/usr/bin/env node
/**
 * CLI executable entry point for web2local.
 *
 * This is the Node.js shebang entry point that bootstraps the CLI.
 * It imports and invokes the main function from the index module.
 *
 * @packageDocumentation
 */
import { main } from './index.js';

main();
