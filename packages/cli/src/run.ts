#!/usr/bin/env node
/**
 * CLI executable entry point.
 *
 * This is the main entry script that gets executed when running the `web2local`
 * command. It simply invokes the main function from the index module.
 */
import { main } from './index.js';

main();
